#!/usr/bin/env bash
# Example: generate token via CTFd UI required. This script is placeholder to remind workflow.
echo "Generate Admin token in CTFd UI -> Settings -> Access Tokens, then paste into .env CT D_ADMIN_TOKEN"
